# Development Environment Configuration
# This file is used when ENVIRONMENT=development (default)

# Environment
ENVIRONMENT=development

# Server
PORT=8000

# Database (Development)
# Connects to local PostgreSQL development database
DATABASE_URL=postgresql://postgres:password@localhost:5432/tonystack_dev

# CORS - Allow local development origins
ALLOWED_ORIGINS=http://localhost:3000,http://localhost:5173,http://localhost:8000

# Logging
LOG_LEVEL=debug

# User Credentials (REQUIRED for auth system)
# Generate secure passwords: openssl rand -base64 32
SUPERADMIN_EMAIL=dev-admin@example.com
SUPERADMIN_PASSWORD=DevPassword123!
ALPHA_EMAIL=alpha@example.com
ALPHA_PASSWORD=AlphaPassword123!
ALPHA_USERNAME=Alpha User

# JWT Authentication (if using --with-auth)
JWT_SECRET=dev-secret-change-in-production
JWT_ISSUER=tonystack
JWT_EXPIRY=1h

# Optional: External Services (Development)
# EMAIL_SERVICE_API_KEY=dev-email-key
# S3_BUCKET_NAME=dev-bucket
