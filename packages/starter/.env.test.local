# Test Environment Configuration
# This file is used when ENVIRONMENT=test (for running tests)

# Environment
ENVIRONMENT=test

# Server (Test)
PORT=8001

# Database (Test)
# Separate database for tests - prevents polluting development data
# Tests will automatically create/drop this database
DATABASE_URL=postgresql://postgres:password@localhost:5432/tstack_starter_test_db

# CORS - Test environment
ALLOWED_ORIGINS=http://localhost:8000,http://localhost:8001

# Logging - Reduced for tests
LOG_LEVEL=error

# JWT Authentication (Test)
# Use simpler secret for tests
JWT_SECRET=test-secret-for-testing-only
JWT_ISSUER=tonystack-test
JWT_EXPIRY=1d

# User Credentials (test-specific)
SUPERADMIN_EMAIL=test-admin@test.local
SUPERADMIN_PASSWORD=TestPassword123!
ALPHA_EMAIL=test-user@test.local
ALPHA_PASSWORD=TestPassword123!
ALPHA_USERNAME=Test User

# Optional: Mock External Services (Test)
# EMAIL_SERVICE_API_KEY=test-email-key
# S3_BUCKET_NAME=test-bucket
