# Production Environment Configuration
# This file is a TEMPLATE - DO NOT commit actual production secrets to git!
# Copy this to .env.production.local and fill in real values

# Environment
ENVIRONMENT=production

# Server
PORT=8000

# Database (Production)
# IMPORTANT: Replace with your actual production database credentials
# Database naming convention: {project_name}_prod
DATABASE_URL=postgresql://your-prod-user:your-prod-password@your-db-host:5432/your_project_prod

# CORS - Production domains only
# Replace with your actual frontend URLs
ALLOWED_ORIGINS=https://your-domain.com,https://www.your-domain.com

# Logging
LOG_LEVEL=info

# User Credentials (PRODUCTION)
# CRITICAL: Change these immediately!
# Generate secure passwords: openssl rand -base64 32
SUPERADMIN_EMAIL=admin@yourcompany.com
SUPERADMIN_PASSWORD=REPLACE_WITH_STRONG_PASSWORD
ALPHA_EMAIL=user@yourcompany.com
ALPHA_PASSWORD=REPLACE_WITH_STRONG_PASSWORD
ALPHA_USERNAME=Production User

# JWT Authentication (Production)
# CRITICAL: Generate a strong random secret!
# Example: openssl rand -hex 64
JWT_SECRET=REPLACE-WITH-STRONG-RANDOM-SECRET-MINIMUM-64-CHARS
JWT_ISSUER=tonystack
JWT_EXPIRY=7d

# Optional: External Services (Production)
# EMAIL_SERVICE_API_KEY=prod-email-key
# S3_BUCKET_NAME=prod-bucket
# S3_ACCESS_KEY=prod-s3-access-key
# S3_SECRET_KEY=prod-s3-secret-key

# Security Notes:
# - Never commit .env.production.local to version control
# - Use environment variables in CI/CD instead of files
# - Rotate secrets regularly
# - Use different credentials for each environment
