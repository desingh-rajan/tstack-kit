# Development image — hot reload via --watch
# Source code is volume-mounted by docker-compose, not copied here.

FROM denoland/deno:latest

WORKDIR /app

# Cache dependencies at build time
COPY deno.json deno.lock* ./
RUN deno install

EXPOSE 8000

CMD ["run", "--allow-all", "--watch", "src/main.ts"]
