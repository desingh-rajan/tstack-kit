# Status Page Configuration

# Server port
PORT=8001

# Check interval in seconds
CHECK_INTERVAL_SECONDS=60

# Check timeout in milliseconds
CHECK_TIMEOUT_MS=5000

# Days of history to keep
HISTORY_DAYS=90

# ---------------------------------------------------------------------------
# Service URLs
# ---------------------------------------------------------------------------
# Local development (Vite dev server ports):
#   API_URL=http://localhost:8000
#   STOREFRONT_URL=http://localhost:5173
#   ADMIN_URL=http://localhost:5174
#
# Production (Docker internal network aliases -- status page is in the same
# Docker network as the other services, so it reaches them directly without
# going through the public-facing kamal-proxy):
#   API_URL=http://myapp-api-internal:8000
#   STOREFRONT_URL=http://myapp-store-internal:8000
#   ADMIN_URL=http://myapp-admin-internal:8000
# ---------------------------------------------------------------------------
API_URL=http://localhost:8000
STOREFRONT_URL=http://localhost:5173
ADMIN_URL=http://localhost:5174

# Health check path per service (default: /health).
# Override only when a service exposes health on a different path.
# Note: kamal-proxy strips the path_prefix before forwarding, so /health
# works for all services even when deployed behind a prefix like /sc-be.
#API_HEALTH_PATH=/health
#STOREFRONT_HEALTH_PATH=/health
#ADMIN_HEALTH_PATH=/health

# Display title
SITE_TITLE=My App Status

# Environment
ENVIRONMENT=development
