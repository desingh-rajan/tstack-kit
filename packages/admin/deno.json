{
  "name": "@tstack/admin",
  "version": "2.1.0",
  "exports": "./mod.ts",
  "license": "MIT",
  "description": "Production-ready admin CRUD API for TStack Kit. Pure JSON API with pagination, search, sorting, and role-based access control. Framework-agnostic, ORM-agnostic architecture.",
  "tasks": {
    "test": "ENVIRONMENT=test deno test --allow-all --no-check --trace-leaks src/_test_setup.ts src/ ; deno run --allow-all scripts/cleanup-test-db.ts",
    "test:watch": "ENVIRONMENT=test deno test --allow-all --no-check --watch src/_test_setup.ts src/",
    "test:coverage": "ENVIRONMENT=test deno test --allow-all --no-check --coverage=coverage src/_test_setup.ts src/ ; deno run --allow-all scripts/cleanup-test-db.ts",
    "cleanup:test-db": "deno run --allow-all scripts/cleanup-test-db.ts",
    "fmt": "deno fmt",
    "lint": "deno lint"
  },
  "imports": {
    "@std/assert": "jsr:@std/assert@^1.0.0",
    "@std/dotenv": "jsr:@std/dotenv@^0.225.0",
    "@std/path": "jsr:@std/path@^1.0.0",
    "hono": "jsr:@hono/hono@^4.6.0",
    "drizzle-orm": "npm:drizzle-orm@^0.36.0",
    "drizzle-orm/pg-core": "npm:drizzle-orm@^0.36.0/pg-core",
    "drizzle-orm/postgres-js": "npm:drizzle-orm@^0.36.0/postgres-js",
    "postgres": "npm:postgres@^3.4.0"
  },
  "fmt": {
    "semiColons": true,
    "singleQuote": false
  },
  "lint": {
    "rules": {
      "tags": ["recommended", "workspace"],
      "include": ["no-unversioned-import", "no-import-prefix"]
    }
  },
  "compilerOptions": {
    "lib": ["deno.window"],
    "strict": true
  },
  "publish": {
    "include": ["mod.ts", "src/**/*.ts", "README.md", "LICENSE"],
    "exclude": ["**/*test.ts", "**/_test_setup.ts"]
  },
  "unstable": ["kv"]
}
